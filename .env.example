# ─── Bitacora App Dashboard — Environment Variables ───────────────
# Copy this file to .env and fill in the values.

# ─── Server (required in production) ─────────────────────────────

# Session secret — any random string (used to sign session cookies)
SESSION_SECRET=change-me-to-a-random-string

# Encryption key — 64 hex characters (32 bytes) for AES-256-GCM
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY=

# YouTrack instance URL (no trailing slash)
YOUTRACK_URL=https://bitacora.youtrack.cloud

# Server port (default: 8080)
PORT=8080

# ─── Frontend env vars (Vite — only needed for dev mode) ──────────
# These are NOT needed in production (backend proxies API calls)
# VITE_YT_TOKEN=perm-...
# VITE_YT_URL=https://bitacora.youtrack.cloud
# VITE_ANTHROPIC_KEY=sk-ant-...
# VITE_OPENAI_KEY=sk-...
